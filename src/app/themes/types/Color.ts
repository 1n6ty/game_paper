export interface Color {
  primary: string;
  background: string;
  surface: string;
  surfaceSecondary: string;
  textPrimary: string;
  textSecondary: string;
  textOnPrimary: string;
  border: string;
  success: string;
  onSuccess: string;
  error: string;
  onError: string;
  overlay: string;
}
